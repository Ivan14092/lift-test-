framework:
    messenger:
        transports:
            async:
                dsn: '%env(REDIS_URL)%'
                options:
                    stream: messages

        routing:
            App\Message\CreateUserRecordMessage: async
# when@test:
#    framework:
#        messenger:
#            transports:
#                # replace with your transport name here (e.g., my_transport: 'in-memory://')
#                # For more Messenger testing tools, see https://github.com/zenstruck/messenger-test
#                async: 'in-memory://'
